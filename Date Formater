
DateTime timegetter = DateTime.now();
var convertedDateTime =
        "${timegetter!.hour.toString().padLeft(2, '0')}-${timegetter!.minute.toString().padLeft(2, '0')}-${timegetter!.second.toString().padLeft(2, '0')}"
            .obs;
